/**
 * Page-Codegen 步骤5：代码质量验证提示词
 */

import { ChatPromptTemplate } from '@langchain/core/prompts';
import { CORE_CONSTRAINTS } from './skill.js';

/**
 * 步骤5：代码质量验证提示词
 */
export const validatePrompt = ChatPromptTemplate.fromMessages([
  ['system', `# 步骤5：代码质量验证

**触发条件**：\`current_step = 5\`

**前置准备（不可跳过）**：\`Read /path/to/booking/task_plan.md\` + \`Read /path/to/booking/research_notes.md\`（回顾整个任务）

${CORE_CONSTRAINTS}

## 核心执行原则
1. Monorepo项目使用 \`pnpm --filter <project-name> run check\`
2. 单项目使用 \`cd /path/to/project && pnpm run check\`（注意&&连接符）
3. 验证失败时修复错误并重新验证，直到零错误

## 🔴 交付前核心约束检查（不可跳过，违反即视为失败）

在执行任何交付操作前，必须逐项确认：

**原始文件完整性检查**：
- [ ] 原始静态模板文件完全未被修改
- [ ] 原始静态模板文件的文件内容、修改时间、权限均未变化
- [ ] 没有使用 Edit 工具修改原始静态模板文件
- [ ] 没有使用 Write 工具覆盖原始静态模板文件

**AI 副本独立性检查**：
- [ ] 所有代码补全工作仅在 AI 工作副本中完成
- [ ] 没有将 AI 副本代码复制到原始文件
- [ ] 没有将 AI 副本代码合并到原始文件
- [ ] 没有建议用户"用 AI 副本替换原始文件"

**文件操作检查**：
- [ ] 没有使用任何文件操作工具（Edit/Write/Bash 复制命令）触碰原始静态模板文件
- [ ] 所有生成和修改的文件都是 AI 工作副本（.ai.tsx）
- [ ] final_code.md 中明确声明原始文件未被修改

**如果以上任何一项检查失败，立即停止并报告错误，禁止继续交付流程**

---

## 强制检查点

**禁止行为**：
- **禁止**跳过代码质量验证步骤
- **禁止**在验证失败时继续到最终交付
- **禁止**猜测或假设验证结果
- **禁止**使用主观判断代替实际验证
- **禁止**将 AI 工作副本代码复制到原始静态模板文件
- **禁止**使用 Edit/Write 工具修改原始静态模板文件
- **禁止**建议用户"用 AI 副本替换原始文件"或类似操作

**必须行为**：
- **必须**实际运行 \`pnpm run check\` 或等效命令
- **必须**等待命令执行完成并获取真实输出
- **必须**根据实际验证结果决定下一步
- **必须**在验证失败时修复所有错误并重新验证
- **必须**确保原始静态模板文件完全未被修改
- **必须**确保所有代码补全仅在 AI 副本中完成
- **必须**在最终交付时明确声明原始文件未被修改

---

## 逐步验证流程

### 验证步骤1：执行代码质量检查

**找到项目根目录（包含package.json），然后按优先级尝试：**

\`\`\`bash
# 方式1：Monorepo项目（推荐）
pnpm --filter <project-name> run check

# 方式2：单项目（使用&&保持shell上下文）
cd /path/to/project && pnpm run check

# 方式3：检查可用脚本
cd /path/to/project && cat package.json | grep -E '"(check|lint|validate|test)"'
\`\`\`

### 验证步骤2：分析验证结果

**退出码为0且无错误** → ✅ 进入最终交付

**退出码非0或存在错误** → 修复错误并重新验证

### 验证步骤3：修复错误（仅验证失败时）

**缺失依赖文件** → 生成缺失的文件（不要删除引用代码）

**代码逻辑错误** → 修改代码解决错误

**重新验证直到零错误**

---

## 最终交付（仅限验证通过后）

**前置条件检查**：
- [ ] 代码质量验证已通过（退出码0，无错误）
- [ ] 所有错误已修复并重新验证
- [ ] 验证命令的完整输出已记录

**如果以上任何一项为否，返回验证步骤，禁止继续**

### 交付步骤1：生成最终代码文件

生成 \`/path/to/booking/final_code.md\`

**文件内容包含**：
- 完整的代码实现（AI 工作副本的代码）
- 详细的代码注释
- 必要的使用说明
- 验证通过的确认信息
- **明确声明**：原始静态模板文件未被修改，所有代码在 AI 副本中完成
- **重要提醒**：请使用 AI 工作副本进行后续开发，不要修改原始静态模板文件

### 交付步骤2：更新任务计划

**更新内容**：
- 标记阶段5为完成：\`- [x] 阶段5：代码质量验证与最终交付\`
- 在"遇到的错误"部分记录验证过程中的问题及解决方案
- 更新状态为："阶段5完成，进入步骤6"

### 交付步骤3：自动进入步骤6

**确认以下条件**：
- [ ] final_code.md 已生成
- [ ] task_plan.md 已更新
- [ ] 代码质量验证通过

**满足条件后自动进入步骤6**

---

## 验证通过标准

**代码质量验证通过的定义**：
- ✅ 验证命令退出码为0
- ✅ 输出中无TypeScript error
- ✅ 输出中无ESLint error（warning可接受，但应尽量修复）
- ✅ 所有已知的代码缺陷已修复
- ✅ 代码符合项目编码规范

**不满足以上任何一项，都视为验证失败，禁止进入最终交付**`],
  ['human', `## 当前任务计划
{taskPlan}

## 当前研究笔记
{researchNotes}

## AI 工作副本文件
{aiWorkFiles}

## 原始静态模板文件
{originalFiles}

## 项目路径
{projectPath}

## 项目名称
{projectName}

请执行步骤5的代码质量验证工作。`],
]);

/**
 * 错误修复提示词
 */
export const errorFixPrompt = ChatPromptTemplate.fromMessages([
  ['system', `你是一个代码调试专家。请根据验证错误，分析问题并提供修复方案。

**重要约束**：
1. 只能修改 AI 工作副本（.ai.tsx），不能修改原始静态模板文件
2. 修复后需要重新验证
3. 如果是缺失依赖文件，需要生成对应的文件

输出修复方案和修改后的代码。`],
  ['human', `## 验证错误日志
{errorLog}

## 相关文件
{relevantFiles}

## AI 工作副本代码
{aiWorkCode}

请分析错误并提供修复方案。`],
]);
